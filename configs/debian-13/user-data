#cloud-config
autoinstall:
  version: 1
  
  refresh-installer:
    update: false
    channel: false
  
  locale: en_US.UTF-8
  
  keyboard:
    layout: us
    variant: ''
  
  network:
    version: 2
    ethernets:
      any:
        match:
          name: "e*"
        dhcp4: true
        dhcp-identifier: mac
  
  apt:
    preserve_sources_list: true
    disable_suites: [updates, backports, security]
  
  storage:
    layout:
      name: lvm
    swap:
      size: 0
  
  identity:
    hostname: debian-13
    username: packer
    password: "$6$rounds=4096$aQ7lbUNhb$O.hZhFvVyxFWWrNQ0g8wJb7c0xXuiXm2l8VJ2u3KM7LJdYhEpkYE0dVq7aWUhJBLmPFzZQ5WoMn2lQ4vQ1cJk0"  # packer
  
  ssh:
    install-server: true
    allow-pw: true
    authorized-keys: []
  
  # Only install packages available on the ISO
  packages:
    - openssh-server
    - sudo
    - open-vm-tools
  
  user-data:
    disable_root: false
  
  late-commands:
    - echo 'root:packer' | chpasswd
    - usermod -aG sudo packer
    - systemctl enable ssh
    - sed -i 's/^#PermitRootLogin.*/PermitRootLogin yes/' /target/etc/ssh/sshd_config
    - sed -i 's/^#PubkeyAuthentication.*/PubkeyAuthentication yes/' /target/etc/ssh/sshd_config
    - echo 'APT::Get::Assume-Yes "true";' > /target/etc/apt/apt.conf.d/90packer
    - echo 'APT::Get::Force-Yes "true";' >> /target/etc/apt/apt.conf.d/90packer
